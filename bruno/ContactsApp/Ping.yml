info:
  name: Ping
  type: http
  seq: 1

http:
  method: GET
  url: "{{urlBase}}/api/index.php/ping"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
