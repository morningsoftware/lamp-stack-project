info:
  name: Get stats
  type: http
  seq: 2

http:
  method: GET
  url: "{{urlBase}}/api/index.php/admin/stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
